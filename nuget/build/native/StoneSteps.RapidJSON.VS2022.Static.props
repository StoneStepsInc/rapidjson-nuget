<?xml version="1.0" encoding="utf-8"?>
<Project DefaultTargets="Build" ToolsVersion="14.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup Label="UserMacros">
    <!-- rapidjson name for all build configurations -->
    <RapidJSONName>rapidjson</RapidJSONName>
    <!-- Define a macro that can be used in project files to reference package directories -->
    <RapidJSONDir>$(MSBuildThisFileDirectory)</RapidJSONDir>
  </PropertyGroup>
</Project>